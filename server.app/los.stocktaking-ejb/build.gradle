sourceSets {
  main {
    java.srcDir 'src'
    resources.srcDir 'src'
  }
  test {
    java.srcDir 'test/groovy'
    resources.srcDir 'src'
  }
}

dependencies {
  compile project(':server.app:los.inventory-ejb')
}

jar {
  archiveName 'los.stocktaking-comp.jar'
  from 'META-INF'
}
